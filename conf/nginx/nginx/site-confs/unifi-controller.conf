server {
    include server-base.conf;
    include client-side-cert-auth.conf;
    add_header X-Robots-Tag "noindex, nofollow, nosnippet, noarchive";

    server_name unifi.*;
    location / {
        include /config/nginx/proxy.conf;
        proxy_pass https://192.168.0.15:8443/;
    }
}
